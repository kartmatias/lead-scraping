# Lead Scraping — Project Context

**Path:** /mnt/operacional/projects-2026/lead-scraping  
**Stack:** Laravel (PHP), SQLite (dev), Redis (queues), Guzzle HTTP, Vite + Tailwind CSS

## Architecture

Queue-based async pipeline:
1. `POST /api/scrape-requests` → creates ScrapeRequest (pending)
2. `RunApifyActor` job → calls Apify API to start actor run, stores apify_run_id + apify_dataset_id
3. `ProcessScrapeRequest` job → polls Apify run status, fetches dataset items when SUCCEEDED (uses `$this->release(30)` for polling)
4. `Lead::upsertFromScrapeData()` → upserts leads by `(source_type, source_id)` unique key

## Apify Actors

| Source      | Actor ID                            |
|-------------|-------------------------------------|
| google_maps | compass/crawler-google-places       |
| instagram   | apify/instagram-scraper             |
| linkedin    | dev_fusion/linkedin-profile-scraper |
| cnpj        | parseforge/brazil-cnpj-scraper      |

## Key Files

- `app/Services/ApifyService.php` — Guzzle wrapper: startActorRun, getRunStatus, getDatasetItems, getAllDatasetItems, cancelRun, getActorDetails
- `app/Jobs/RunApifyActor.php` — triggers actor runs; actor_id resolved via match() on scrapeRequest->source
- `app/Jobs/ProcessScrapeRequest.php` — polls + persists leads (tries=3, backoff=30)
- `app/Models/Lead.php` — upsertFromScrapeData(), belongsTo ScrapeRequest
- `app/Models/ScrapeRequest.php` — startScrape(), complete(), fail(), cancel(), isPending(), isRunning()
- `app/Providers/ApifyActorRegistry.php` — actor list; uses `~` separator (e.g. `compass~crawler-google-places`)
- `app/Providers/ApifyActorValidationServiceProvider.php` — validates all actors at boot; skipped if APIFY_VALIDATION_DISABLED=true or testing env
- `routes/api.php` — /api/scrape-requests (CRUD + status + cancel), /api/leads (index, stats, export, show, destroy)

## DB Schema

**scrape_requests:** id, source (enum: google_maps/instagram/linkedin/cnpj), status (enum: pending/running/completed/failed/cancelled), filters (JSON), apify_run_id, apify_dataset_id, total_leads, completed_leads, failed_leads, started_at, completed_at, error_message, timestamps

**leads:** id, scrape_request_id (FK cascade), source_type, source_id, name, email, phone, company, position, address, cnpj, website, instagram, linkedin, facebook, raw_data (JSON), timestamps  
UNIQUE: (source_type, source_id)

## Notes

- Actor IDs in ApifyActorRegistry use `~` but RunApifyActor uses `/` — inconsistency
- Webhook support NOT implemented (polling only)
- No authentication on API routes
- Env: APIFY_TOKEN required; read via config('services.apify.token')
- Tests: Unit (ScrapeRequest, Lead, RunApifyActor, ProcessScrapeRequest, ApifyService) + Feature (LeadController, ScrapeRequestController)
- `docs/superpowers/specs/` and `docs/superpowers/plans/` — design docs
- `bootstraper.md`, `definitions.md` — architecture notes
- `adhoc-webhook.md` — webhook notes
- Git branches: main, feature, agents
